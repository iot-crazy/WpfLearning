<UserControl x:Class="wpfgfx.CountdownButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfgfx"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard >
                    <Int32Animation Storyboard.Target="{Binding .}" Storyboard.TargetProperty="CurrentSeconds" From="{Binding Seconds}" To="0" Duration="{Binding Duration}"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Grid>
        <Button x:Name="m_Button" IsEnabled="{Binding IsEnabled}" Width="150" Height="25">
            <Button.Content>
                <TextBlock x:Name="m_TextBlock" Text="{Binding DisplayMessage}" />
            </Button.Content>
            
        </Button>
    </Grid>
</UserControl>
